---
__default__: minor
---

Add native Node-API packages for Apple silicon macOS, x64 glibc Linux, and x64 Windows while retaining the browser WebAssembly entry. Ambiguous sidecar basenames now require an exact path or resolver result instead of silently selecting the first match.
