---
title: Options
description: Configure the target format, sidecar resolution, and export properties.
---

<auto-type-table path="./props.ts" name="ConvertOptions" />

`to` is an assimp exporter id plus the aliases `glb`, `gltf`, `step`, and
`dae`. `properties` is the escape hatch onto assimp's own export properties,
such as `3MF_EXPORT_UNIT`.

<auto-type-table path="./props.ts" name="CreateAssimpOptions" />
